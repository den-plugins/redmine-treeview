REDMINE TREEVIEW PLUGIN

features:
  = show tree view of features and tasks (collapsible/expandable)
  = filter issues
  = select visible columns
  = default visible columns: tracker, subject, assigned to, status, target version, story points
  = add new feature/task
  
dependencies:
  = redmine_custom plugin
